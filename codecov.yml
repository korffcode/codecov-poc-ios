coverage:
  status:
    patch:
      default:
        target: 80%
        threshold: 10%
    project:
      default:
        # basic
        target: 90%
        threshold: 10%
        if_ci_failed: error
        informational: false
        only_pulls: false
